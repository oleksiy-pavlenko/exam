# Page Transcripts

Page transcripts are close to the scanned source. They preserve visible text, formulas, exercise numbers, and figure labels in Finnish.

Current chapters:
- [BOOK01 CH01](BOOK01/CH01/index.md)

Rules:
- One page file per source page ID.
- Each page file records canonical `Book pages` and `Book page basis` metadata plus the raw `Printed pages visible` note.
- Use stable line IDs such as `L001`.
- Mark unclear text with the standard uncertainty marker from `AGENTS.md`.
- Derived notes must cite page IDs and line IDs from this layer.
